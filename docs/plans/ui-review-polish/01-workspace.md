# Phase A: workspace

- Put the existing queue before compact metrics.
- Use `queue.read.total` for the headline and the ranked displayed rows for an explicitly scoped unknown-deadline count.
- Keep all case rows, ordering, search, permission text, and truncated-list explanations.
- Make the retailer/customer prominent and identifiers secondary.
- Verify empty, singular, unknown-deadline, and truncated states.
