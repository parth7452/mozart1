node_modules/
.next/
dist/
coverage/
.turbo/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Next.js build output, and the standalone preview render (regenerate with
# `pnpm render:web` — it needs a database, so it is not a source artifact).
apps/web/.next
apps/web/.cache
apps/web/preview
apps/web/next-env.d.ts
apps/web/.env.local
